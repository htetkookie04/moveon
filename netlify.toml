# Netlify config for Kookie Move On frontend (Vite + React SPA)

[build]
  base = "frontend"
  command = "npm run build"
  publish = "dist"

# SPA: serve index.html for all routes so React Router can handle them
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
